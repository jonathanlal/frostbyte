import { ConfigType } from '@stitches/react/types/config';

export type FrostbyteConfigType = {
  prefix?: ConfigType.Prefix;
  media?: ConfigType.Media;
  theme?: ConfigType.Theme;
  themeMap?: ConfigType.ThemeMap;
  utils?: ConfigType.Utils;
};
